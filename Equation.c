void Schodinger(){
    int pai = 3.1415926; //circle constant
    int h = 6.62607015e-34; //Planck's constant
    char H = "Hamiltonian";
    char imagenary_number = sqrt(-1);
    const i = imagenary_number;
     "i * h / 2 * pai = H * (fai)";
}
void Maxwell(){
     int general();{
     char B,E,D,H;
     const B = "magnetic";
     const E = "electron";
     const D = "Electric Displacement Vector";
     const H = "Magnetic Field Strength";
     }
     float relative();{
     char dotproduct = ".";
     char crossproduct = "::"; 
     char curl = "->";
     char div = "<>";
     const relative_magnetic_permeability = "rmp";
     const relative_electric_permeability = "rep";
}
    double XYZT();{
     char x,y,z,t;
     const x = "x[i]";
     const y = "y[j]";
     const z = "z[k]";
     const t = "t[l]";
     char gradle = "(x,y,z,t)::(0,0,0,0)";
    }
    return gradle("<><><><>");
    return gradleXYZT("i->j->k->l");
}
void Newton(){
      Newton_First_Law("Inertia");{
        /* 
           "Newton's first law states that an object at rest stays at rest,
           and an object in motion stays in motion with the same speed and in the same direction unless acted upon by a net external force,
           This law is also known as the law of inertia, which implies that objects tend to resist changes in their velocity"
       */
      }
      Newton_Second_Law("Acceleration");{
       /*
       Newton's second law describes the relationship between an object's mass, 
       its acceleration, and the net force acting on it. It can be expressed by the formula F = ma, 
       where F is the net force acting on the object, m is the mass of the object, and a is the acceleration produced. 
       This law essentially means that the acceleration of an object is directly proportional to the net force acting on the object and inversely proportional to its mass.
       */
      ;}
      Newton_Third_Law("Action and Reaction");{
        /*
        Newton's third law states that for every action, there is an equal and opposite reaction. 
          This means that if object A exerts a force on object B, then object B will exert an equal but opposite force back on object A. 
          These forces always occur in pairs, and the two forces are always of the same magnitude and opposite in direction.
        */ 
      ;}
      return "F = mass * velocity";
;}
void Dirac(){
/*
The Dirac equation is a fundamental equation in quantum mechanics that describes the behavior of spin - 1/2 particles, such as electrons and quarks. 
It was formulated by British physicist Paul Dirac in 1928. The equation combines special relativity with quantum mechanics, 
resulting in a wave function that has four components corresponding to the particle's spin states.
The Dirac equation can be written in the following form:
(i * ��^0 * (E * ��^0 - p * ��^1) - m) * �� = 0
The key feature of the Dirac equation is its prediction of antimatter. 
For every solution to the Dirac equation, there exists an anti-particle with the same mass but opposite charge. 
This discovery resolved the issue of negative energy states predicted by earlier versions of quantum theory and laid the foundation for the development of quantum field theory.
The Dirac equation also leads to the concept of spinors, which are mathematical objects used to describe the intrinsic angular momentum (spin) of elementary particles. 
Spinors have properties that distinguish them from ordinary vectors or tensors, such as their transformation under Lorentz transformations.
In summary, the Dirac equation is a pivotal advancement in theoretical physics that unifies the principles of special relativity with quantum mechanics, 
leading to the prediction of antiparticles and providing a framework for understanding the spin of fundamental particles.
*/
}
